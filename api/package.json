{
  "name": "ict-bot-api",
  "version": "1.0.0",
  "description": "FastAPI backend for ICT_BOT trading system",
  "scripts": {
    "start": "uvicorn main:app --reload --host 0.0.0.0 --port 8000"
  }
}
